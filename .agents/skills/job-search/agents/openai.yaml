interface:
  display_name: "Job Search"
  short_description: "Search, screen, normalize, and log roles."
  default_prompt: "Use $job-search to find or validate roles, log each decision, and hand strong fits to $job-apply."
